# CS370-Lab-3
An executable window that will draw a 10x10 grid, within that grid should be lines of random colors and lengths. A button labeled “Redraw” centered at the bottom of the window, which when activated will randomly redraw the lines again. 
